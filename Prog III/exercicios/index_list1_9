/*9. Contar Ocorrências de um Valor

Crie uma função que conte quantas vezes um valor específico aparece em um array usando forEach.

Entrada: [1, 2, 2, 3, 2, 4], valor = 2

Saída esperada: 3 */
let numeros= [1,2,2,3,2,4];
let valor = 2;

/*let ocorrencia=0

numeros.forEach(numero => numero==valor?ocorrencia++:"");
console.log(ocorrencia);*/

//usando o filter()
let ocorrencia = numeros.filter(numero => numero == valor).length;
console.log(ocorrencia);